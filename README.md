# Projeto de Automação de Testes - SauceDemo 🧪

Este projeto é um projeto de aprendizagem que demonstra a automação de fluxos críticos (E2E) e testes de API utilizando **Cypress**. O objetivo é garantir a qualidade e a estabilidade das principais funcionalidades da plataforma SauceDemo.

## 🚀 Tecnologias Utilizadas
* **Cypress** (Automação de interface e API)
* **JavaScript** (Lógica de testes)
* **Node.js** (Ambiente de execução)

## 📋 Cenários Implementados
* **Login:** Sucesso e validação de mensagens de erro.
* **E2E:** Fluxo completo de compra (da seleção ao checkout).
* **API:** Validação de contratos e status codes (GET 200/404).

## 🛠️ Como executar
1. Clone o projeto: `git clone https://github.com/willamsbarbosa/automacao-cypress-saucedemo.git`
2. Instale as dependências: `npm install`
3. Abra o Cypress: `npx cypress open` ou execute em modo headless: `npx cypress run`
